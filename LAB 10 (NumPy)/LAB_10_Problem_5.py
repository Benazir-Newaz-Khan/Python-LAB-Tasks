import numpy as np

x = np.array([2, 4, 7, 11])
y = np.array([1, 2, 3, 5])

print("Summation:", np.sum(x))
print("Product:", np.prod(x))
print("Difference:", np.diff(x))
print("Summation of two arrays:", np.sum(x + y))
